import { Button } from "@g-ui/components";

const Basic = () => {
  return (
    <>
      <Button type="default" dashed>
        Button
      </Button>
      <Button type="primary" dashed>
        Button
      </Button>
      <Button type="warning" dashed>
        Button
      </Button>
      <Button type="success" dashed>
        Button
      </Button>
      <Button type="error" dashed>
        Button
      </Button>
    </>
  );
};

export default Basic;
