import { Button } from "@g-ui/components";

const Basic = () => {
  return (
    <>
      <Button size="small" dashed>
        小按钮
      </Button>
      <Button>按钮</Button>
      <Button size="large">大按钮</Button>
    </>
  );
};

export default Basic;
