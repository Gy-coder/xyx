import Basic from "./basic";
import Controlled from "./controlled";
import YearMonthPicker from "./yearMonthPicker";
import Status from "./status";
import Size from "./size";
import DemoBlock from "../../components/DemoBlock";

# DatePicker 日期选择器

<DemoBlock>

    <Basic />

    ```tsx
    +1
    ```

</DemoBlock>

<DemoBlock>

    <Controlled />

    ```tsx
    +1
    ```

</DemoBlock>

<DemoBlock>

    <YearMonthPicker />

    ```tsx
    import { DatePicker } from '@g-ui/components'

    const Demo = () => {
        return <>
            月份选择: <DatePicker onChange={(newValue, valueString) => console.log(newValue, valueString)} picker="month" />
            <br />
            <br />
            年份选择: <DatePicker onChange={(newValue, valueString) => console.log(newValue, valueString)} picker="year"/>
        </>
    }

    export default Demo
    ```

</DemoBlock>

<DemoBlock>

    <Status />

    ```tsx
    import { DatePicker } from '@g-ui/components'

    const Demo = () => {
        return <>
            error 状态: <DatePicker onChange={(newValue, valueString) => console.log(newValue, valueString)} status="error" />
            <br />
            <br />
            warning状态: <DatePicker onChange={(newValue, valueString) => console.log(newValue, valueString)} status="warning" />
        </>

    }

    export default Demo
    ```

</DemoBlock>

<DemoBlock>

    <Size />

    ```tsx
    import { DatePicker } from '@g-ui/components'

    const Demo = () => {
        return <>
            <DatePicker size="small" onChange={(newValue, valueString) => console.log(newValue, valueString)} />
            <br />
            <br />
            <DatePicker size="middle" onChange={(newValue, valueString) => console.log(newValue, valueString)} />
            <br />
            <br />
            <DatePicker size="large" onChange={(newValue, valueString) => console.log(newValue, valueString)} />
        </>
    }

    export default Demo
    ```

</DemoBlock>
